
let carrera = {
    habilitarVehiculo(
        let habilitado = autos.buscarPorPatente(patente).sancionado
        habilitado = habilitado ? false : false
        return autos.buscarPorPatente(patente)
    )
}